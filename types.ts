// Points to a date type. will list all methods available to date types
const today = new Date();

// plain object. will only list defined properties
const person = {
	age: 20,
};

class Color {}

// variable pointing to type Color. has access to all properties and methods of Class 'color'
const red = new Color();
